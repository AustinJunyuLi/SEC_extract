# Raw extraction artifacts

These are the clean reference-9 raw extractor outputs used by the April 23, 2026 adjudication pass.
They were originally written to `/tmp/sec_extract_ref9_clean/{slug}.raw.json` and copied here so the handoff is fetchable from GitHub.

Finalized pipeline outputs live in `output/extractions/{slug}.json`; adjudication reports live one directory up.
